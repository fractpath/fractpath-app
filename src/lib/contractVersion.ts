// Single source of truth for canonical snapshot versioning in the App repo.
// Keep these aligned with the widget + compute contract.

export const CONTRACT_VERSION = "10.2.0";
export const SCHEMA_VERSION = "1";
